-- YAML configuration files
return {
  lsp = {
    "yamlls", -- YAML LSP
  },
  formatters = {
    "prettier", -- YAML formatter (shared with other languages)
  },
  linters = {
    "yamllint", -- YAML linter
  },
}
